diff --git a/src/rrdtool.c b/src/rrdtool.c
index e29d637..dc03476 100644
--- a/src/rrdtool.c
+++ b/src/rrdtool.c
@@ -61,6 +61,7 @@ typedef struct rrd_queue_s rrd_queue_t;
 static const char *config_keys[] = {
     "CacheTimeout", "CacheFlush",      "CreateFilesAsync", "DataDir",
     "StepSize",     "HeartBeat",       "RRARows",          "RRATimespan",
+	"RRASingle",
     "XFF",          "WritesPerSecond", "RandomTimeout"};
 static int config_keys_num = STATIC_ARRAY_SIZE(config_keys);
 
@@ -78,8 +79,10 @@ static rrdcreate_config_t rrdcreate_config = {
     /* timespans = */ NULL,
     /* timespans_num = */ 0,
 
-    /* consolidation_functions = */ NULL,
-    /* consolidation_functions_num = */ 0,
+	/* rrasingle = */ 0,
+
+	/* consolidation_functions = */ NULL,
+	/* consolidation_functions_num = */ 0,
 
     /* async = */ 0};
 
@@ -936,6 +939,11 @@ static int rrd_config(const char *key, const char *value) {
       return (1);
     }
     rrdcreate_config.rrarows = tmp;
+  } else if (strcasecmp ("RRASingle", key) == 0) {
+    if (IS_TRUE (value)) {
+        rrdcreate_config.rrasingle = 1;
+        NOTICE ("rrdtool plugin: RRASingle = true: creating only AVERAGE RRAs");
+    }
   } else if (strcasecmp("RRATimespan", key) == 0) {
     char *saveptr = NULL;
     char *dummy;
diff --git a/src/utils_rrdcreate.c b/src/utils_rrdcreate.c
index 750d265..ab7c2f9 100644
--- a/src/utils_rrdcreate.c
+++ b/src/utils_rrdcreate.c
@@ -180,6 +180,9 @@ static int rra_get(char ***ret, const value_list_t *vl, /* {{{ */
     rts_num = rra_timespans_num;
   }
 
+  if (cfg->rrasingle)
+    rra_types_num = 1;
+
   rra_max = rts_num * rra_types_num;
   assert(rra_max > 0);
 
diff --git a/src/utils_rrdcreate.h b/src/utils_rrdcreate.h
index 9bc67ae..8e1b327 100644
--- a/src/utils_rrdcreate.h
+++ b/src/utils_rrdcreate.h
@@ -40,6 +40,8 @@ struct rrdcreate_config_s {
   int *timespans;
   size_t timespans_num;
 
+  int rrasingle;
+
   char **consolidation_functions;
   size_t consolidation_functions_num;
 
